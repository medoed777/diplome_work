services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: referral_system
    command: sh -c "python manage.py migrate && python manage.py collectstatic --noinput && python manage.py add_users && python manage.py csu && gunicorn config.wsgi:application --bind 0.0.0.0:8000"
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    environment:
      DATABASE_HOST: db
      REDIS_HOST: redis
    network_mode: host
    volumes:
      - .:/app
      - static_volume:/app/static/
    depends_on:
      - db
      - redis

  db:
    image: postgres:16.3
    ports:
      - 5432:5432
    container_name: postgres_diplome
    env_file:
      - ./.env
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      DATABASE_HOST: db
      REDIS_HOST: redis
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: redis-diplome
    environment:
      DATABASE_HOST: db
      REDIS_HOST: redis


volumes:
  postgres_data:
  static_volume: